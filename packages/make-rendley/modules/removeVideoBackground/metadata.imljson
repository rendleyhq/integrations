{
	"label": "Remove Video Background",
	"description": "Removes the background from a video, keeping the foreground subject. Returns a job_id immediately. Poll 'Get Job Status' (Rendley API) until status is 'completed'. The finished job carries a fresh download_url (Get Job Status), and media_id for 'Get Media URL'."
}
