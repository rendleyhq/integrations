{
	"label": "Generate Image",
	"description": "Generates an image from a text prompt (optionally guided by an input image). Returns a job_id immediately. Poll 'Get Job Status' (Rendley API) until status is 'completed'. The finished job carries a fresh download_url (Get Job Status), and media_id for 'Get Media URL'."
}
