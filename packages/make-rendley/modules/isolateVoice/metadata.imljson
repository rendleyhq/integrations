{
	"label": "Isolate Voice",
	"description": "Removes background noise and music from a recording, keeping only the voice. Returns a job_id immediately. Poll 'Get Job Status' (Rendley API) until status is 'completed'. The finished job carries a fresh download_url (Get Job Status), and media_id for 'Get Media URL'."
}
