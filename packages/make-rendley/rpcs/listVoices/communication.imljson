{
	"url": "/ai/text-to-speech/voices",
	"method": "GET",
	"qs": {
		"limit": 100
	},
	"response": {
		"limit": 300,
		"iterate": "{{body.data}}",
		"output": {
			"label": "{{item.name}}",
			"value": "{{item.id}}"
		}
	}
}
